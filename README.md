# 🐧 Ubuntu 24 Terminal

Works inside any Linux container — no Docker needed. Uses proot.

## Quick Start

```bash
curl -sL https://raw.githubusercontent.com/vabshi8-cpu/bookish-lamp/main/install.sh | bash
